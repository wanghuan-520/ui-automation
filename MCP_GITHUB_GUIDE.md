# 🔧 使用MCP GitHub工具提交代码

## 📊 仓库信息

- **Owner**: wanghuan-520
- **Repo**: ui-automation
- **Branch**: main
- **Remote**: https://github.com/wanghuan-520/ui-automation.git

---

## ⚠️ MCP工具的限制

### MCP GitHub工具适合：
- ✅ 单个文件的创建/更新
- ✅ 少量文件的批量推送
- ✅ 远程仓库的查看和管理

### MCP GitHub工具不适合：
- ❌ 大量文件的批量提交（效率低）
- ❌ 复杂的Git操作（如合并、变基）
- ❌ 本地Git历史的管理

---

## 💡 推荐方案

### 方案A: 使用Git命令（推荐）⭐⭐⭐⭐⭐

**在新终端执行**：
```bash
cd /Users/wanghuan/aelf/Cursor/ui-automation
bash cleanup.sh
git add .
git commit -m "feat: 优化测试框架"
git push
```

**优点**：
- 快速高效
- 保留完整的Git历史
- 支持所有Git功能

---

### 方案B: 使用MCP推送关键文件（备选）

使用MCP工具逐个推送最重要的文件：

#### 主要文件清单：
1. `run_smoke_test.py` - 快速测试脚本
2. `run_all_tests_parallel.py` - 并行测试脚本
3. `.gitignore` - Git配置
4. `cleanup.sh` - 清理脚本
5. `README_SIMPLIFIED.md` - 快速指南

**缺点**：
- 需要逐个文件推送
- 无法批量处理
- 效率较低

---

### 方案C: 使用GitHub CLI（如果已安装）

```bash
cd /Users/wanghuan/aelf/Cursor/ui-automation
bash cleanup.sh
git add .
git commit -m "feat: 优化测试框架"
gh repo view  # 查看仓库
git push
```

---

## 🎯 最终建议

由于你有**大量文件**需要提交，最好的方式是：

### 立即执行（复制到新终端）：

```bash
cd /Users/wanghuan/aelf/Cursor/ui-automation && \
bash cleanup.sh && \
git add . && \
git commit -m "feat: 优化测试框架

- 添加run_smoke_test.py快速测试（1-3分钟）
- 添加run_all_tests_parallel.py并行测试（20-30分钟）
- 优化.gitignore，排除截图和报告
- 添加cleanup.sh清理脚本
- 简化文档结构" && \
git push origin main
```

---

## 📝 如果必须使用MCP

如果你坚持使用MCP工具，我可以帮你逐个推送文件，但这会：
1. 花费更长时间（需要推送10+个文件）
2. 需要读取每个文件的内容
3. 每个文件一次API调用
4. 无法保留完整的Git提交历史

**建议**: 还是使用标准Git命令更合适。

---

## ✅ Git命令执行步骤

### 步骤1: 打开Terminal
- Command + Space
- 输入 "Terminal"

### 步骤2: 执行命令
复制粘贴：
```bash
cd /Users/wanghuan/aelf/Cursor/ui-automation
bash cleanup.sh
git add .
git commit -m "feat: 优化测试框架"
git push
```

### 步骤3: 验证
```bash
git log --oneline -3
git status
```

---

## 🌐 访问远程仓库

提交后访问：
https://github.com/wanghuan-520/ui-automation

---

**最后更新**: 2025-10-30  
**推荐方案**: 使用Git命令（方案A）

